import { expect } from 'chai';
import emod from '../../src/fns/emod';

describe('fns/emod', function () {
  it('should be a function', function () {
    expect(typeof emod).to.equal('function');
  });
});
