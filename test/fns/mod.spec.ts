import { expect } from 'chai';
import mod from '../../src/fns/mod';

describe('fns/mod', function () {
  it('should be a function', function () {
    expect(typeof mod).to.equal('function');
  });
});
