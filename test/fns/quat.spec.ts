import { expect } from 'chai';
import quat from '../../src/fns/quat';

describe('fns/quat', function () {
  it('should be a function', function () {
    expect(typeof quat).to.equal('function');
  });
});
