import { expect } from 'chai';
import vec3 from '../../src/fns/vec3';

describe('fns/vec3', function () {
  it('should be a function', function () {
    expect(typeof vec3).to.equal('function');
  });
});
