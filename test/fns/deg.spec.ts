import { expect } from 'chai';
import deg from '../../src/fns/deg';

describe('fns/deg', function () {
  it('should be a function', function () {
    expect(typeof deg).to.equal('function');
  });
});
