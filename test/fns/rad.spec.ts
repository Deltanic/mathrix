import { expect } from 'chai';
import rad from '../../src/fns/rad';

describe('fns/rad', function () {
  it('should be a function', function () {
    expect(typeof rad).to.equal('function');
  });
});
