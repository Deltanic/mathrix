import { expect } from 'chai';
import Vector3 from '../../src/primitives/Vector3';

describe('primitives/Vector3', function () {
  it('should be a function', function () {
    expect(typeof Vector3).to.equal('function');
  });
});
