import { expect } from 'chai';
import Vector2 from '../../src/primitives/Vector2';

describe('primitives/Vector2', function () {
  it('should be a function', function () {
    expect(typeof Vector2).to.equal('function');
  });
});
