import { expect } from 'chai';
import Matrix4 from '../../src/primitives/Matrix4';

describe('primitives/Matrix4', function () {
  it('should be a function', function () {
    expect(typeof Matrix4).to.equal('function');
  });
});
