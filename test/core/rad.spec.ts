import { expect } from 'chai';
import rad from '../../src/core/rad';

describe('core/rad', function () {
  it('should be a function', function () {
    expect(typeof rad).to.equal('function');
  });
});
