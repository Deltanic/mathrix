import { expect } from 'chai';
import deg from '../../src/core/deg';

describe('core/deg', function () {
  it('should be a function', function () {
    expect(typeof deg).to.equal('function');
  });
});
