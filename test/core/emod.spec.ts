import { expect } from 'chai';
import emod from '../../src/core/emod';

describe('core/emod', function () {
  it('should be a function', function () {
    expect(typeof emod).to.equal('function');
  });
});
