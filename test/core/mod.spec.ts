import { expect } from 'chai';
import mod from '../../src/core/mod';

describe('core/mod', function () {
  it('should be a function', function () {
    expect(typeof mod).to.equal('function');
  });
});
